services:
  mkdocs:
    image: docker.io/binarycodes/mkdocs:latest
    container_name: mkdocs
    volumes:
      - .:/blog:ro
    ports:
      - "8000:8000"
    # the site is built at start; restart the container to publish new posts
    restart: unless-stopped
